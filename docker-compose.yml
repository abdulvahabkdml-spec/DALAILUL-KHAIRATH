services:
  mongodb:
    image: mongodb/mongodb-community-server:latest
    container_name: dk-web-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
